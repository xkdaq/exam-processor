---
name: exam-processor
description: 考研真题处理器 - 自动识别真题内容，生成答案，并按模板格式输出Excel。当用户上传真题文件（Word/PDF/图片）并要求整理成Excel时使用此技能。
---

# 考研真题处理器

## 功能概述

处理考研真题，自动完成以下任务：
1. 识别真题内容（支持Word、PDF、图片格式）
2. 如用户未提供答案，自动生成完整答案
3. 按标准模板格式输出Excel文件

## 使用场景

当用户：
- 上传真题文件（.docx/.pdf/.jpg/.png）
- 说"整理成Excel""做成题库""生成答案"等
- 需要按特定模板格式输出

## Excel模板格式规范

字段顺序及要求：
| 字段 | 说明 |
|------|------|
| ID | 题目序号，从1开始递增。**材料题例外，见下方"材料题 ID 编号规则"** |
| 题目 | 完整题目内容 |
| 题型 | 单选题/多选题/问答题/材料题。除单选、多选、材料题母行外，名词解释/简答题/论述题/案例分析题/判断说理题等一律填"问答题" |
| 分数 | 题目分值（如有） |
| 难度 | 留空 |
| 选项A-E | 客观题填写，主观题留空 |
| 答案 | 客观题填答案，主观题留空 |
| 解析 | 完整答案/解析内容 |
| 一级目录 | 年份+学校+科目，如"26年福建师范大学810" |
| 二级目录 | 实际具体题型，如"名词解释/简答题/论述题/案例分析题/判断说理题" |

### 材料题 ID 编号规则（重要）

材料题（一个材料带若干小问）**不按行号递增编号**，必须拆成「母行 + 子行」，
并用两级序号建立从属关系。假设该材料题在卷面上的序号是 `N`：

| 行 | ID | 题型 | 分数 | 题目 | 解析 |
|----|----|------|------|------|------|
| 母行 | `N` | 材料题 | 各小问分值之和 | 材料正文（**不含**小问） | 留空 |
| 子行1 | `N.1` | 小问的实际题型 | 该小问分值 | `(1)xxx` | 该小问答案 |
| 子行2 | `N.2` | 小问的实际题型 | 该小问分值 | `(2)xxx` | 该小问答案 |
| 子行3 | `N.3` | 小问的实际题型 | 该小问分值 | `(3)xxx` | 该小问答案 |

规则要点：

1. **ID 跟题号走，不跟行号走**。材料题后面若还有题目，其 ID 继续用卷面序号
   `N+1`、`N+2`...，**不能**因为前面插入了子行而顺延。
   正确序列示例：`1 2 3 4 5 5.1 5.2 5.3 6 7 8`。
2. 子行 ID 写作 `N.1` / `N.2` / `N.3`，依次累加（数值型即可，如 `5.1`）。
3. 子行的**题型按小问实际类型**填写：小问是客观题就填"单选题"/"多选题"并补齐
   选项A-E与答案；是主观题就填"问答题"。**不要**一律写成"问答题"。
4. 子行的**题目文本必须带小括号编号**（如 `(1)...` / `（1）...`）。导入系统靠
   小括号识别从属关系，缺了括号会被判成一道独立题目。
5. 母行的二级目录填原始题型（如"材料分析题"）；子行二级目录与母行保持一致。

参考模板：`~/Desktop/题库excel模板.xlsx`（见 ID 为 5 / 5.1 / 5.2 / 5.3 的示例行）。

## 处理流程

### 1. 读取输入文件

根据文件类型选择读取方式：
- **Word (.docx)**: 使用 `python-docx` 读取段落（脚本: `scripts/read_docx.py`）
- **PDF (.pdf)**: 
  - 普通PDF: 使用 `pdfplumber` 提取文本
  - 扫描版PDF: 使用OCR识别（脚本: `scripts/extract_pdf_ocr.py`）
- **图片 (.jpg/.png)**: 使用OCR识别

#### OCR提取扫描版PDF

对于扫描版PDF（图片型PDF），使用 `extract_pdf_ocr.py` 脚本：

```bash
python scripts/extract_pdf_ocr.py <pdf路径> [输出路径] [最大页数]
```

示例：
```bash
python scripts/extract_pdf_ocr.py 真题.pdf 输出.txt 10
```

依赖安装：
```bash
pip install PyMuPDF Pillow pytesseract
# macOS系统还需安装tesseract引擎
brew install tesseract tesseract-lang
```

#### 还原 PDF 折行（文字版 PDF 必做）

`extract_text()` 会按 PDF 里的物理行换行，导致题干和解析出现句中硬换行
（如"能提\n高行为频率"），必须还原成自然段落后再入库。用 `pdfplumber` 的
行几何信息判断段落边界：

```python
import pdfplumber

def extract_items(pdf_path):
    raw = []
    with pdfplumber.open(pdf_path) as pdf:
        for p in pdf.pages:
            for l in p.extract_text_lines():
                t = l['text'].strip()
                if not t or t.isdigit():       # 丢弃空行与纯页码行
                    continue
                raw.append((t, round(l['x0'], 1), round(l['x1'], 1)))
    # 正文右边缘 = 出现次数最多的 x1（两端对齐的满行都收在此处）
    right = max(set(x for _, _, x in raw),
                key=lambda x: sum(1 for _, _, y in raw if y == x))
    thresh = right - 5.3          # 满行与段末行之间有明显空档，取中值
    base_x0 = min(x0 for _, x0, _ in raw)   # 正文左边界
    items, para, prev_short = [], -1, True
    for t, x0, x1 in raw:
        if prev_short or x0 > base_x0 + 8:    # 上一行是段末行 / 本行段首缩进
            para += 1
        items.append({'text': t, 'para': para})
        prev_short = x1 < thresh
    return items

def to_text(items):               # 同段直接拼接，段间换行
    paras, cur = [], None
    for it in items:
        if cur is None or it['para'] != cur:
            paras.append(it['text']); cur = it['para']
        else:
            paras[-1] += it['text']
    return '\n'.join(paras)
```

判定要点：
- **段首缩进**最可靠：正文续行贴左边界（`x0 = base`），新段落缩进两格
  （`x0 ≈ base + 24`）。仅靠它会漏掉无缩进的段落。
- **段末短行**：段落最后一行明显短于右边缘。阈值须落在"最大段末行"与
  "最小满行"之间的空档里，两者通常只差几 pt，务必先打印 `[右边缘-20, 右边缘)`
  区间的行人工确认阈值。
- 两者取并集即可覆盖。答案区常整段不缩进，此时只靠短行规则生效。
- 固定标记行（`【参考答案】`、`1.【答案】C。`、题型标题）另加一条强制断段规则。
- **选项必须按物理行解析**，不能先合并段落：合并后 `A.xxx` 与 `B.xxx` 之间
  没有空格，将无法切分。

#### 合并后再二次切段（答案区必做）

几何规则无法识别"上一段以。结尾、下一段以 `N.【答案】` 开头"这类紧贴的
答案块，合并后它们会粘成一整段（如 `...规律。正确答案为B。8.【答案】A。...`）。
必须在合并完成后再按标记二次切段，放在 `to_text()` 末尾：

```python
SPLIT_SEG = re.compile(r'(?<=[。])\s*(?=\d{1,2}\s*[.．]?\s*[【\[])')

def to_paras(items):
    paras, cur = [], None
    for it in items:                      # 先按几何合并
        if cur is None or it['para'] != cur:
            paras.append(it['text']); cur = it['para']
        else:
            paras[-1] += it['text']
    out = []
    for p in paras:                       # 再按答案标记二次切段
        for seg in SPLIT_SEG.split(p):
            s = seg.strip()
            if s:
                out.append(s)
    return out
```

答案块编号的正则要容忍 PDF 抽出的异常空格（`16【. 答案】C。`）：

```python
ANS_MARK = re.compile(r'^(\d{1,2})\s*[.．]?\s*[【\[]\s*[.．]?\s*答案】')
REF_MARK = re.compile(r'^(\d{1,2})\s*[.．]?\s*【参考答案】')
```

#### 切块计数器的经典坑

按"题号 == 已收集块数 + 1"判断是否开启新块会**漏切**：当前块还没 append，
块数一直少 1，第二题就匹配不上。必须单独维护期望题号：

```python
blocks, cur, nxt = [], None, 1
for ln in lines:
    m = re.match(r'^(\d{1,2})[.．](?!\d)(.*)$', ln, re.S)
    if m and int(m.group(1)) == nxt:
        nxt += 1                      # 先递增，再收上一块
        if cur is not None:
            blocks.append(cur)
        cur = [m.group(2)]
    else:
        cur.append(ln)
```

选项切分用 `(?<![\w])([A-E])[.．]` 做 `re.split`，可同时吃掉三种排版：
独立成行、同行两个选项（`A.xx B.xx`）、紧贴题干（`...（ ）A.xx`）。

### 2. 解析题目结构

识别并提取：
- 年份信息
- 题型分类（名词解释/简答题/论述题等）
- 各题的题目内容
- 分值信息（如有）

### 3. 判断答案来源

- **用户已提供答案**：提取并使用用户答案
- **用户未提供答案**：调用知识库生成完整答案

### 4. 生成答案内容

生成答案时遵循：
- 名词解释：定义+特征+分类+例证+意义
- 简答题：分点论述，条理清晰
- 论述题：论点+论据+分析+总结，层次分明
- 解析中适当换行，提高可读性

### 5. 输出Excel文件

使用 `pandas` + `openpyxl` 生成Excel：
```python
import pandas as pd

# 处理题型字段逻辑
def process_question_type(actual_type):
    """
    处理题型字段：
    - 单选题/单项选择题 -> 统一为"单选题"
    - 多选题/多项选择题 -> 统一为"多选题"
    - 其他所有题型（名词解释/简答题/论述题/案例分析题/判断说理题/辨析题等）-> 统一为"问答题"
    注意：原始题型可能有多种写法（如"单项选择题"/"单选题"），必须全部覆盖。
    """
    if actual_type in ['单选题', '单项选择题']:
        return '单选题'
    elif actual_type in ['多选题', '多项选择题']:
        return '多选题'
    else:
        return '问答题'

# 构建数据时，题型列使用处理后的值，二级目录保留原始具体题型
for q in questions:
    q['题型'] = process_question_type(q['实际题型'])  # 单选/多选保持，其他变为"问答题"
    q['二级目录'] = q['实际题型']  # 保留具体题型如"名词解释/简答题/论述题"

df = pd.DataFrame(data)
df.to_excel(output_path, index=False, engine='openpyxl')
```

**题型字段处理规则**：
- 原始题型为"单选题"或"单项选择题" → 题型列填"单选题"，二级目录保留原始写法
- 原始题型为"多选题"或"多项选择题" → 题型列填"多选题"，二级目录保留原始写法
- 原始题型为"名词解释" → 题型列填"问答题"，二级目录填"名词解释"
- 原始题型为"简答题" → 题型列填"问答题"，二级目录填"简答题"
- 原始题型为"论述题" → 题型列填"问答题"，二级目录填"论述题"
- 原始题型为"分析论述题" → 题型列填"问答题"，二级目录填"分析论述题"
- 原始题型为"案例分析题" → 题型列填"问答题"，二级目录填"案例分析题"
- 原始题型为"判断说理题" → 题型列填"问答题"，二级目录填"判断说理题"
- 原始题型为"辨析题" → 题型列填"问答题"，二级目录填"辨析题"
- 原始题型为"材料题"/"材料分析题" → 母行题型列填"材料题"，二级目录填"材料分析题"；
  子行题型列按小问实际类型填（单选题/多选题/问答题），二级目录同母行填"材料分析题"，
  子行 ID 用 `N.1`/`N.2`/`N.3`（详见"材料题 ID 编号规则"）

> ⚠️ **常见陷阱**：真题文档中"单项选择题""多项选择题"是"单选题""多选题"的常见变体写法，`process_question_type` 必须同时匹配两种写法，否则单选题/多选题会被误判为"问答题"。

## 支持的科目类型

- 文学类：古代文学、现当代文学、文学理论、比较文学
- 语言学类：现代汉语、古代汉语、语言学概论
- 教育类：教育学、教育实践与方法
- 其他人文社科类

## 注意事项

1. 答案生成应准确、专业、完整
2. 解析列需要适当换行排版
3. 保持用户原有题目内容不变
4. 如有多年的真题，可合并到一个Excel，用"一级目录"区分年份
5. 临时脚本文件用完后及时清理

## 示例用法

用户："@真题.docx 整理成Excel"
→ 读取docx → 解析题目 → 生成答案 → 输出Excel

用户："@图片1.jpg @图片2.jpg 生成题库"
→ OCR识别 → 解析题目 → 生成答案 → 输出Excel
